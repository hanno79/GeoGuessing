{
  "name": "geoguessing",
  "version": "1.0.0",
  "private": true,
  "description": "GeoGuessing – Browser-based geo guessing game",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "topojson-client": "^3.1.0",
    "world-atlas": "^2.0.2"
  }
}
